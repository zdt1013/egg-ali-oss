'use strict';

exports.keys = '123';
exports.aliOss = {
  client: {
    cluster: [
      Object.assign({}, require('../../../../config')),
      Object.assign({}, require('../../../../config')),
    ],
  },
};
